# Non-canonical staging

The files under `chapters/` and `_direct_payload/` were created during an abandoned attempt to mirror the hash-pinned direct artifact into GitHub. They are incomplete and are **not** the W1 learner handoff.

The canonical W1 artifact identity is recorded in `direct-handoff.json` and `../qa/final.json` under `contracts/DIRECT_HANDOFF.md`.
