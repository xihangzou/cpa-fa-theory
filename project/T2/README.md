# T2 — 財務会計論テキスト　理論2

Editable project template only. No inventory or semantic QA has passed.

Run the linked `T2` inventory issue first. It owns `sources.json`, `batches.json`, `source-map.json`, the local ignored cache and inventory review. Conversion issues own isolated `chunks/ISSUE-ID/`, `fragments/ISSUE-ID.json`, and `reviews/ISSUE-ID.json`. Chapter reviewers own their assigned corrections and one record per chapter. The assembly issue alone populates canonical `tasks.json` from approved shards.

Future outputs: `merged/textbook.md`, `merged/chapter-*.md`, `merged/assembly.json`, and `qa/final.json`. Do not create these during repository setup.

See [records](../../contracts/RECORDS.md), [source contract](../../SOURCES.md) and [issue index](../../ISSUES.md).
